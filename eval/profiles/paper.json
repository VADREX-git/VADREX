{
  "name": "paper",
  "description": "Reproduces the paper evaluation: full-mode defaults (repeats=10, the complete set of scales, intervals, anchor counts and study sizes), about 10.7 hours as measured. The six measurement groups run sequentially in a fixed order; verify-cost and grace depend on accumulated anchor state, so they must not be reordered or run in parallel.",
  "mode": "full",
  "repeats": 10,
  "env": {},
  "expect": {
    "totalRows": 360,
    "rowsByFile": {
      "scaling.json": 120,
      "revocation_delay.json": 40,
      "handshake_overhead.json": 30,
      "verify_cost.json": 40,
      "grace_window.json": 40,
      "attack_injection.json": 90
    },
    "requireZeroFailures": true,
    "requireScalingVerified": true,
    "attackLabels": { "S1": 4, "S2": 3, "S3": 2 },
    "requireFigures": true,
    "requireCleanTree": true
  }
}
