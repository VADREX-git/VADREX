{
  "name": "full",
  "description": "Regression check before a paper run. repeats=1 at reduced scale, but keeping the code paths only the full run exercises (200 MiB handshake, 50-anchor verify cost, every attack case). The wait-dominated points (300s anchor interval, 100 anchors, 1M leaves) are excluded: they add runtime over the same code paths.",
  "mode": "full",
  "repeats": 1,
  "env": {
    "EVAL_LOG_SIZES": "1000,10000,100000",
    "EVAL_ANCHOR_INTERVALS": "10,30",
    "EVAL_ANCHOR_COUNTS": "1,50",
    "EVAL_STUDY_SIZES": "small:5242880,large:209715200"
  },
  "expect": {
    "totalRows": 26,
    "requireZeroFailures": true,
    "requireScalingVerified": true,
    "attackLabels": { "S1": 4, "S2": 3, "S3": 2 },
    "requireFigures": true,
    "requireCleanTree": false
  }
}
